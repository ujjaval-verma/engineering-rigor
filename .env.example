# Copy to .env (gitignored). Defaults work without it.
# `just` loads this file into every recipe (`set dotenv-load := true`). Never set
# PYTEST_ADDOPTS here — `just env-guard` rejects it, and the gate must be deterministic.
APP_NAME=example_app
LOG_LEVEL=INFO
